# Broker MQTT de dev. Em produção: listener TLS com require_certificate true —
# certificado X.509 individual por sensor/PLC (identidade por dispositivo).
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
