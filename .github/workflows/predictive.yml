name: predictive

on:
  pull_request:
    paths: ['src/Predictive/**', 'src/Platform/**', 'schemas/**']
  push:
    branches: [main]
    paths: ['src/Predictive/**', 'src/Platform/**', 'schemas/**']

jobs:
  ci:
    uses: ./.github/workflows/_dotnet-service.yml
    with:
      service-path: src/Predictive
      service-name: predictive
      coverage-threshold: 80
