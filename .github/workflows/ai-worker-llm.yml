name: ai-worker-llm

on:
  pull_request:
    paths: ['src/Ai/**', 'src/Platform/**', 'schemas/**']
  push:
    branches: [main]
    paths: ['src/Ai/**', 'src/Platform/**', 'schemas/**']

jobs:
  ci:
    uses: ./.github/workflows/_dotnet-service.yml
    permissions:
      contents: read
      packages: write
      id-token: write
    with:
      service-path: src/Ai
      service-name: ai-worker-llm
      coverage-threshold: 80
      dockerfile: src/Ai/Dockerfile.worker-llm
