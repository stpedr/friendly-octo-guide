name: data-archiver

on:
  workflow_dispatch: # re-run manual (ex.: exercitar o job de imagem na main)
  pull_request:
    paths: ['src/Data.Archiver/**', 'src/Platform/**', 'schemas/**']
  push:
    branches: [main]
    paths: ['src/Data.Archiver/**', 'src/Platform/**', 'schemas/**']

jobs:
  ci:
    uses: ./.github/workflows/_dotnet-service.yml
    permissions:
      contents: read
      packages: write
      id-token: write
    with:
      service-path: src/Data.Archiver
      service-name: data-archiver
      coverage-threshold: 80
