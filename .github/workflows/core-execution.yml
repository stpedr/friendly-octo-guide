name: core-execution

on:
  pull_request:
    paths: ['src/Core.Execution/**', 'src/Platform/**', 'schemas/**']
  push:
    branches: [main]
    paths: ['src/Core.Execution/**', 'src/Platform/**', 'schemas/**']

jobs:
  ci:
    uses: ./.github/workflows/_dotnet-service.yml
    permissions:
      contents: read
      packages: write
      id-token: write
    with:
      service-path: src/Core.Execution
      service-name: core-execution
      coverage-threshold: 80
